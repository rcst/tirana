---
title: "Template Title"
author: "Your Name"
date: '`r format(Sys.Date(), format = "%d. %B %Y")`'
output:
  xaringan::moon_reader:
    css: ["default", "tiranatransport.css", "tiranatransport-fonts.css"]
    nature:
      ratio: "16:9"
      highlightStyle: github
      highlightLines: true
      # highlightLanguage: ["r"]
      countIncrementalSlides: false
      titleSlideClass: [middle, right, inverse]
    includes:
      after_body: [insert-logo.html]
params:
  www: "https://rcst.netlify.app/doc/stats-training/07-computing-and-plotting-slides.pdf"
  # www: "https://bit.ly/2VLCLR6"
---

```{r setup, include=FALSE}
library(ggplot2)
library(data.table)
library(qrencoder)
knitr::opts_chunk$set(echo = TRUE, 
		      dpi = 300)

# png("img/qr.png", width = 500, height = 500)
# qrcode_gen(dataString = params$www)
# dev.off()

# generate qr code svg
f <- file("img/qr.svg")
writeLines(text = qrencode_svg(to_encode = params$www), con = f)
close(f)
```

# Introduction

This is the introduction slide

This presentation can be found under `r params$www`.

```{r, echo=FALSE}
knitr::include_graphics("img/qr.svg")
```


---

# Analysis

This is the analysis slide

```{r}
rnorm(10)
```

---

# Visualizations

.pull-left[
```{r carsplot, fig.show = 'hide'}
ggplot(data = mtcars, mapping = aes(x = mpg, y = hp, color = factor(cyl))) +
  geom_point()
```
]

.pull-right[
```{r, echo = FALSE}
knitr::include_graphics(
			knitr::fig_chunk("carsplot", "png"))
```
]

---

# Conclusion

This is the conclusion slide
